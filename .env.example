# Coheara — Environment Variables
# ================================
# Copy to .env and customize. NEVER commit .env files.
# All variables are optional — the app works without them.

# ── Logging ──────────────────────────────────────────────
# Override default log verbosity.
# Dev default: coheara=debug | Prod default: coheara=warn
# Syntax: https://docs.rs/tracing-subscriber/latest/tracing_subscriber/filter/struct.EnvFilter.html
# RUST_LOG=coheara=debug

# ── Build Signing (production builds only) ───────────────
# Required by: ./build.sh desktop (without --no-sign)
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
# ANDROID_KEYSTORE_PASSWORD=
# ANDROID_KEY_PASSWORD=
# ANDROID_KEY_ALIAS=coheara
